## Example Company

#### Example Job Title

Example detailed paragraph describing a specific project or responsibility
at this job — this is the "detailed" variant of a snippet that also has a
shorter "standard" variant defined in `cv/web/data.yaml`.

Another example paragraph. Replace this whole file with real detail, or
delete it — `content/work-experience/*.md` is optional, additional detail
for the snippet library beyond what's in `data.yaml`.
