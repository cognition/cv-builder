[behave]
# Default suite exercises the real Flask app + APIs under features/*.feature.
# Wireframe prototype (in-memory) suite:
#   behave features/wireframe --no-capture
paths = features
default_tags = ~@wireframe
show_skipped = false
stdout_capture = false
stderr_capture = false
logging_level = INFO
format = plain
